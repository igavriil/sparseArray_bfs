on void readFromFile(struct matrix* sparseArray)
sparseArray.c line 453

you have to change the path of the sparse.txt file
	fp = fopen("/home/tsou/Desktop/structures_new/sparseArray/sparse.txt","r")

sparsearray.txt needs minimum size for the sparse Array 30
